interface PostPrevNext {
  prevPost?: {
    title: string;
    link: string;
  };
  nextPost?: {
    title: string;
    link: string;
  };
}

export default function PostPrevNext({ prevPost, nextPost }: PostPrevNext) {
    return <nav>
        
    </nav>
}
